[project]
name = "mubi-recommendations"
version = "0.1.0"
description = "Fetch new MUBI releases, enrich ratings, export CSV, and optionally notify Telegram."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32,<3.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.0,<10.0",
    "ruff>=0.15.7",
]
